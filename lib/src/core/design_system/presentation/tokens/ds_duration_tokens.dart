abstract class DurationTokens {
  static const Duration shortest = Duration(milliseconds: 200);
  static const Duration shorter = Duration(milliseconds: 400);
  static const Duration short = Duration(milliseconds: 600);
  static const Duration normal = Duration(milliseconds: 800);
  static const Duration long = Duration(milliseconds: 1000);
  static const Duration longer = Duration(milliseconds: 1200);
  static const Duration longest = Duration(milliseconds: 1400);
}
