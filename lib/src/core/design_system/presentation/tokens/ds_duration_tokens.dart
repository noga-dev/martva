abstract class DurationTokens {
  static const Duration quickest = Duration(milliseconds: 200);
  static const Duration quicker = Duration(milliseconds: 400);
  static const Duration quick = Duration(milliseconds: 600);
  static const Duration normal = Duration(milliseconds: 800);
  static const Duration slow = Duration(milliseconds: 1000);
  static const Duration slower = Duration(milliseconds: 1200);
  static const Duration slowest = Duration(milliseconds: 1400);
}
