abstract class DurationTokens {
  static const Duration shortest = Duration(milliseconds: 300);
  static const Duration shorter = Duration(milliseconds: 600);
  static const Duration short = Duration(milliseconds: 900);
  static const Duration normal = Duration(milliseconds: 1200);
  static const Duration long = Duration(milliseconds: 1500);
  static const Duration longer = Duration(milliseconds: 1800);
  static const Duration longest = Duration(milliseconds: 2100);
}
